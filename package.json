{
  "name": "antivirus-lambda",
  "version": "1.0.0",
  "description": "A ClamAV Lambda Layer built with Docker and managed with Serverless",
  "devDependencies": {
    "aws-sdk": "^2.964.0",
    "jest": "^27.0.6"
  },
  "scripts": {
    "build": "./build.sh",
    "deploy": "sls deploy",
    "test": "jest handler.test.js"
  }
}